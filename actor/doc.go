// Package actor implements a single-process service runtime with named
// services, generation-safe references, serialized mailboxes, typed protocol
// descriptors, and cooperative yielding across nested calls.
package actor
